import TechStackClient from "./TechStackClient";

/**
 * 技術スタックセクション（Server Component）
 * データフェッチとSEO最適化を担当
 */
export default function TechStackSection() {
  // Server Componentでのデータ取得（将来の拡張用）
  // const techStackData = await getTechStackData();

  return <TechStackClient />;
}